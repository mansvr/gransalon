# HALLMARK — visual skin

Skill: `O:\Umbral\hallmark\skills\hallmark\SKILL.md`

## What founder asked

- Apply **Hallmark** on this project.
- Homepage **wireframe** from VP B5 (plate + chat).
- **Own design system:** clean, crisp, modern **monochrome** directory (not VP Stone Fresh).

## Receiving-agent procedure

1. **Do not** `hallmark study https://vpandco.co` and then emit VP pixels. Study is for DNA; we already named the DNA: **asymmetric 2+1 plate, ghost rail, equal-weight catalog**. That is enough.
2. Custom theme is **required**. Founder mood: **clean, crisp, modern directory, monochrome.** Ink / paper / hairline rules. One ink weight for CTAs — no terracotta, no “fresh” green, no official-fair navy, no purple. Still 2+1 fonts (display + body); monochrome is colour, not Inter-only slop.
3. Stamp pre-emit critique on the first `design.md` / `globals.css`.
4. Diversification: this must **not** reuse B5’s exact type pairing (if B5 is Instrument/Geist or Playfair/Inter, pick another 2+1 pairing). New OKLCH tokens.
5. Macrostructure: **not** hero → 3 features → CTA. Use **plate + indexed catalogue** (wayfinding). Heading placement: hanging or overlapping the plate edge — not generic centered SaaS hero.
6. Mobile gates (320–768) are mandatory. Chip row must wrap; no horizontal page scroll.
7. Honest copy only: **159 empresas** (from 182 official cards) is the real list count. Do not invent “25.000 visitantes ya usan la guía.”

## Suggested `design.md` (emit in the new repo, not here)

After first visual pass, lock tokens in-repo:

- `--color-paper` `--color-ink` `--color-rule` (accent = ink or 10% mix — no second hue)
- `--font-display` `--font-body`
- `--hero-media-h`

## Anti-goals

Purple gradients, Inter-only, fake browser chrome around the iframe, italic display headers, “trusted by 50k.”
